name: Create DataPower yaml files

on: [push]

jobs:
  DataPower_Yaml_Job1:
    name: DataPower yaml Job
    runs-on: ubuntu-latest
    
    steps:
      - name: File contents
        run: echo $(ls)
        shell: bash
      - name: Run a script file from repo
        if: ${{ always() }}
        run: sh ${{ github.action_path }}/yaml-creation.sh
        shell: bash
      - name: Execute during failure
        if: ${{ failure() }}
        run: echo "One of the step seems failed"
        