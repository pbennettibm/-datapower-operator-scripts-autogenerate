echo `git diff --name-only | grep -E '.zip$' `
zipfiles=`git diff --name-only | grep -E '.zip$' `

for file in $zipfiles; do
  rm -r $file-output
  rm -r $file-unpack
  ./migrate-backup.sh $file.zip
  git add $file-unpack
  git add $file-output
done